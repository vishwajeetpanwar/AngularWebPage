import { FilterByStudentCategoryPipe } from './filter-by-student-category.pipe';

describe('FilterByStudentCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByStudentCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
